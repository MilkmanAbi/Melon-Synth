# MLC — Melon Lyric Conversion Engine
# Python dependencies

# Core G2P engine (wraps espeak-ng)
phonemizer>=3.2.1

# Language detection
langdetect>=1.0.9

# NLP toolkit (CMUdict for English stress, tokenisation)
nltk>=3.8.1

# ── System dependency (NOT pip) ──────────────────────────────────────────
# espeak-ng must be installed at the OS level:
#
#   Ubuntu/Debian:  sudo apt install espeak-ng
#   macOS:          brew install espeak
#   Windows:        https://github.com/espeak-ng/espeak-ng/releases
#                   (download the installer, add to PATH)
#   Arch:           sudo pacman -S espeak-ng
#   Fedora:         sudo dnf install espeak-ng
