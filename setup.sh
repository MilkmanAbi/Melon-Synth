#!/bin/bash
# Melon Synth — First-time setup
# Run this once: bash setup.sh

echo "Installing dependencies..."
npm install

# If npm install fails (network issues), try:
# npm install --prefer-offline

echo "Done. Start with: npm run dev"
